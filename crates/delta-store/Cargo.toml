[package]
name = "delta-store"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "In-memory node store. Persistence is a later, optional layer."

[dependencies]
delta-core = { workspace = true }
